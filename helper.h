#ifndef HELPER_H
#define HELPER_H

#include <string>
#include <algorithm>
#include "constant.h"

bool process( char *str );
bool process( std::string& str );
bool reverseComplement( std::string& sequence );

#endif